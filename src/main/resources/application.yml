server:
  port: 9090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pharma?currentSchema=dev
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
